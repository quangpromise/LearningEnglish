/// Flutter plugin version, must be kept in sync with `pubspec.yaml` `version`.
/// Injected into native SDK so OTel `sdk.version` reflects the Flutter package
/// version (not the underlying native SDK version).
const kPluginVersion = '1.3.6';
